# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This repository is currently in its initial state. It is named "Claude" and lives at `renanoacruz-gif/Claude`. Development happens on feature branches and is merged into `main`.

## Branch Conventions

- Feature/task branches follow the pattern `claude/<description>-<id>` (e.g., `claude/claude-md-docs-YBcRm`).
- All development should happen on the designated feature branch, not directly on `main`.

## Notes for Future Sessions

- There are no build, test, or lint commands yet — update this file once a tech stack is chosen.
- Update this file whenever significant architectural decisions are made (language, framework, directory structure, tooling).
